# MediPlus Backend Dependencies
# Full MONAI Medical Imaging Capabilities
# Optimized for Intel i7 Ultra + Iris GPU

# Core Framework
flask>=2.3.0
flask-cors>=4.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Machine Learning - PyTorch (CPU version for Intel optimization)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ===========================================
# MONAI - Medical Open Network for AI (Full)
# ===========================================
monai[all]>=1.3.0

# MONAI Optional Dependencies for Full Capabilities
nibabel>=5.0.0          # NIfTI/DICOM support
pydicom>=2.4.0          # DICOM file handling
itk>=5.3.0              # Advanced image processing
scikit-image>=0.21.0    # Image processing utilities
einops>=0.7.0           # Tensor operations for transformers
transformers>=4.30.0    # For ViT, BERT-based models
tensorboard>=2.14.0     # Training visualization
mlflow>=2.7.0           # Experiment tracking (optional)

# Medical Image Formats
SimpleITK>=2.3.0        # Cross-platform medical image IO
h5py>=3.9.0             # HDF5 support for large datasets
nrrd>=1.0.0             # NRRD format support

# Intel Optimizations (Optional - for better performance on Intel CPUs)
# Uncomment if running on Intel hardware:
# intel-extension-for-pytorch>=2.0.0

# OpenVINO (Optional - for Intel GPU acceleration)
# Uncomment for Intel Iris GPU support:
# openvino>=2023.0.0
# openvino-dev>=2023.0.0

# Image Processing
pillow>=10.0.0
opencv-python-headless>=4.8.0

# API Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Groq AI SDK
groq>=0.4.0

# Production Server
gunicorn>=21.0.0

# Development & Testing
pytest>=7.4.0
pytest-cov>=4.1.0
tqdm>=4.66.0            # Progress bars
