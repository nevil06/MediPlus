# MediPlus Backend Dependencies
# Optimized for Intel i7 Ultra + Iris GPU

# Core Framework
flask>=2.3.0
flask-cors>=4.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Machine Learning - PyTorch (CPU version for Intel optimization)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# MONAI - Medical Open Network for AI
monai>=1.3.0

# Intel Optimizations (Optional - for better performance on Intel CPUs)
# Uncomment if running on Intel hardware:
# intel-extension-for-pytorch>=2.0.0

# OpenVINO (Optional - for Intel GPU acceleration)
# Uncomment for Intel Iris GPU support:
# openvino>=2023.0.0
# openvino-dev>=2023.0.0

# Image Processing (for future ECG/medical image features)
pillow>=10.0.0
opencv-python-headless>=4.8.0

# API Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Production Server
gunicorn>=21.0.0

# Development & Testing
pytest>=7.4.0
pytest-cov>=4.1.0
